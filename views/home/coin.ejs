<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
    <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>
    <!--Plotly CDN-->
  </head>
  <body>
    <%- include('../partials/nav') %>
    
    <div class="container mb-3">

      <h2 class="mb-3">코인 종목 확인</h2>

      <div id="graph">
        <script>
          //Plotly.plot('graph',json복붙,{})
          var data = "<%= data %>";
          data = data.replace(/&#34;/g,'"');
          // console.log(JSON.parse(data)['data']['0']['y'][2]);
          // parsed_data = JSON.parse(data)
          // parsed_data['data']['0']['y'][2] = 50000;
          // console.log(JSON.parse(data)['data']['0']['y'][2]);

          // console.log(typeof (Object.values(JSON.parse(data)['data']['0']['x'])));
          // Plotly.plot('graph',parsed_datd,{});
          Plotly.plot('graph',JSON.parse(data),{});
        </script>
      </div>

    </div>
  </body>
</html>